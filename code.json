{
  "code": "new = '''from enum import Enum as PyEnum\nfrom typing import Self\nfrom uuid import UUID, uuid7\n\nfrom sqlalchemy import UUID as SAUUID, BigInteger, Enum, ForeignKey, Index, Integer, String\nfrom sqlalchemy.dialects.postgresql import JSONB\nfrom sqlalchemy.orm import Mapped, mapped_column\n\nfrom app.chats.config import chat_config\nfrom app.core.db.base_model import BaseModel, DateMixin\n\n\nclass AttachmentType(str, PyEnum):\n    IMAGE = \"image\"\n    VIDEO = \"video\"\n    FILE = \"file\"\n    VOICE = \"voice\"\n    VIDEO_NOTE = \"video_note\"\n\n\nMEDIA_ATTACHMENT_TYPES: frozenset[AttachmentType] = frozenset({\n    AttachmentType.IMAGE,\n    AttachmentType.VIDEO,\n})\n#: Attachment kinds that must be the only attachment of a message (Telegram-like).\nEXCLUSIVE_ATTACHMENT_TYPES: frozenset[AttachmentType] = frozenset({\n    AttachmentType.VOICE,\n    AttachmentType.VIDEO_NOTE,\n})\n\n\nclass AttachmentStatus(str, PyEnum):\n    PENDING = \"pending\"\n    SUCCESS = \"success\"\n    ERROR = \"error\"\n\nclass MessageAttachment(BaseModel, DateMixin):\n    __tablename__ = \"message_attachments\"\n\n    id: Mapped[UUID] = mapped_column(SAUUID(as_uuid=True), primary_key=True)\n    message_id: Mapped[UUID | None] = mapped_column(\n        SAUUID, ForeignKey(\"messages.id\", ondelete=\"CASCADE\"),\n        nullable=True, index=True,\n    )\n    chat_id: Mapped[UUID] = mapped_column(SAUUID(as_uuid=True), nullable=False)\n\n    uploader_id: Mapped[int] = mapped_column(BigInteger, nullable=False)\n\n    attachment_type: Mapped[AttachmentType] = mapped_column(\n        Enum(AttachmentType), nullable=False\n    )\n    attachment_status: Mapped[AttachmentStatus] = mapped_column(\n        Enum(AttachmentStatus), nullable=False\n    )\n\n    s3_key: Mapped[str] = mapped_column(String(512), nullable=False)\n    mime_type: Mapped[str] = mapped_column(String(128), nullable=False)\n    original_filename: Mapped[str] = mapped_column(String(256), nullable=False)\n    size: Mapped[int] = mapped_column(BigInteger, nullable=False)\n\n    width: Mapped[int | None] = mapped_column(Integer, nullable=True)\n    height: Mapped[int | None] = mapped_column(Integer, nullable=True)\n    duration_seconds: Mapped[int | None] = mapped_column(Integer, nullable=True)\n\n    #: Millisecond precision duration, reported by the client for voice/video notes.\n    duration_ms: Mapped[int | None] = mapped_column(Integer, nullable=True)\n    #: Amplitude envelope for voice messages (list of small ints, 0..31).\n    waveform: Mapped[list[int] | None] = mapped_column(JSONB, nullable=True)\n\n    source_attachment_id: Mapped[UUID | None] = mapped_column(SAUUID(as_uuid=True), nullable=True)\n\n    __table_args__ = (\n        Index(\"ix_msg_attachments_message_id\", \"message_id\"),\n        Index(\"ix_msg_attachments_chat_uploader\", \"chat_id\", \"uploader_id\"),\n        Index(\"ix_msg_attachments_type_status\", \"attachment_type\", \"attachment_status\"),\n    )\n\n    @property\n    def is_voice(self) -> bool:\n        return self.attachment_type == AttachmentType.VOICE\n\n    @property\n    def is_video_note(self) -> bool:\n        return self.attachment_type == AttachmentType.VIDEO_NOTE\n\n    @classmethod\n    def create(\n        cls, chat_id: UUID, uploader_id: int,\n        attachment_type: AttachmentType, s3_key: str, mime_type: str,\n        original_filename: str, size: int,\n        duration_ms: int | None = None,\n        waveform: list[int] | None = None,\n    ) -> Self:\n        instance = cls(\n            id=uuid7(),\n            chat_id=chat_id,\n            message_id=None, uploader_id=uploader_id,\n            attachment_type=attachment_type, s3_key=s3_key, mime_type=mime_type,\n            original_filename=original_filename, size=size,\n            attachment_status=AttachmentStatus.PENDING,\n        )\n        if duration_ms is not None:\n            instance.set_duration_ms(duration_ms)\n        if waveform is not None:\n            instance.set_waveform(waveform)\n        return instance\n\n    def create_for_forward(self, chat_id: UUID) -> \"MessageAttachment\":\n        instance = MessageAttachment(\n            id=uuid7(),\n            chat_id=chat_id,\n            uploader_id=self.uploader_id,\n            attachment_type=self.attachment_type, s3_key=self.s3_key, mime_type=self.mime_type,\n            original_filename=self.original_filename, size=self.size,\n            attachment_status=AttachmentStatus.SUCCESS,\n            duration_seconds=self.duration_seconds,\n            duration_ms=self.duration_ms,\n            waveform=self.waveform,\n            width=self.width,\n            height=self.height,\n            source_attachment_id=self.id\n        )\n        return instance\n\n    def set_resolution(self, width: int, height: int) -> None:\n        self.width = width\n        self.height = height\n\n    def set_duration(self, duration_seconds: int) -> None:\n        self.duration_seconds = duration_seconds\n\n    def set_duration_ms(self, duration_ms: int) -> None:\n        self.duration_ms = max(0, int(duration_ms))\n        self.duration_seconds = max(0, round(self.duration_ms / 1000))\n\n    def set_waveform(self, waveform: list[int]) -> None:\n        points = [\n            max(0, min(chat_config.VOICE_WAVEFORM_MAX_VALUE, int(point)))\n            for point in waveform[: chat_config.VOICE_WAVEFORM_MAX_POINTS]\n        ]\n        self.waveform = points or None\n\n    def mark_proccesed(self) -> None:\n        self.attachment_status = AttachmentStatus.SUCCESS\n'''\nopen('/home/user/repo/app/chats/models/attachment.py','w').write(new)\nprint(\"ok\")"
}